@if (Request.IsAuthenticated)
{
    @Html.AntiForgeryToken()
    @Html.ActionLink("Create New Post", "Create", "Postings", routeValues: null, htmlAttributes: new { @class = "btn btn-primary btn-large index-button" })
}
