@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    {
    @Html.AntiForgeryToken()
            @Html.ActionLink("Create New Post", "NewPost", "Home", routeValues: null, htmlAttributes: new { title = "Manage" })
    }
}
